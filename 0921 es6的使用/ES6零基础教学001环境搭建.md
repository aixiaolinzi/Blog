第一天的使用。

##项目构建

####基础架构

####任务自动化（gulp）

####编译工具（babel、webpack）

####代码实现



>Express的使用：

>记得要安装。通过node

> npm install -g express

> npm install -g express-generator

> 想要在命令行中使用express，必须使用上面两条指令。



##基础架构

1.业务逻辑：（页面，交互）

2.自动构建（便宜，辅助）（辅助：：自动刷新，文件合并，资源压缩）

3.服务接口（数据，接口）

##任务自动化（gulp）
1. gulp --watch执行到不停止：
```groovy
tasks/util/args.js
少了最后一行代码：：
export default args;
这样就可以了。
```

2.默认的端口是3000，
3.
